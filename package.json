{
  "name": "semantic-tab-grouper",
  "version": "0.1.0",
  "description": "Agrupa abas do Chrome automaticamente por similaridade semântica de conteúdo, usando embeddings gerados on-device com Transformers.js.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "vite": "^5.4.10"
  }
}
